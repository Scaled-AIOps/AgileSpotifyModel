meta {
  name: Change Password
  type: http
  seq: 6
}

patch {
  url: {{baseUrl}}/api/v1/auth/me/password
  body: json
  auth: bearer
}

auth:bearer {
  token: {{accessToken}}
}

body:json {
  {
    "currentPassword": "Admin1234!",
    "newPassword": "NewPassword123!"
  }
}

meta {
  name: Change Passcode
  type: http
  seq: 6
}

patch {
  url: {{baseUrl}}/api/v1/auth/me/passcode
  body: json
  auth: bearer
}

auth:bearer {
  token: {{accessToken}}
}

body:json {
  {
    "currentPasscode": "Admin1234!",
    "newPasscode": "NewPasscode123!"
  }
}

meta {
  name: List Subdomains
  type: http
  seq: 1
}

get {
  url: {{baseUrl}}/api/v1/subdomains
  body: none
  auth: bearer
}

auth:bearer {
  token: {{accessToken}}
}

script:post-response {
  if (res.status === 200 && res.body.length > 0) {
    bru.setVar("subdomainId", res.body[0].id);
  }
}

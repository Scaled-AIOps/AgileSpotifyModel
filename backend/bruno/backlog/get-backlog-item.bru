meta {
  name: Get Backlog Item
  type: http
  seq: 3
}

get {
  url: {{baseUrl}}/api/v1/squads/{{squadId}}/backlog/{{backlogItemId}}
  body: none
  auth: bearer
}

auth:bearer {
  token: {{accessToken}}
}

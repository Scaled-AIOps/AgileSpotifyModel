meta {
  name: Delete Key Result
  type: http
  seq: 7
}

delete {
  url: {{baseUrl}}/api/v1/squads/{{squadId}}/okrs/{{okrId}}/keyresults/{{krId}}
  body: none
  auth: bearer
}

auth:bearer {
  token: {{accessToken}}
}

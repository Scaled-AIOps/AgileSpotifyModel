meta {
  name: List Squads
  type: http
  seq: 1
}

get {
  url: {{baseUrl}}/api/v1/squads
  body: none
  auth: bearer
}

auth:bearer {
  token: {{accessToken}}
}

script:post-response {
  if (res.status === 200 && res.body.length > 0) {
    bru.setVar("squadId", res.body[0].id);
  }
}

meta {
  name: Add Item to Sprint
  type: http
  seq: 8
}

post {
  url: {{baseUrl}}/api/v1/squads/{{squadId}}/sprints/{{sprintId}}/items/{{backlogItemId}}
  body: none
  auth: bearer
}

auth:bearer {
  token: {{accessToken}}
}

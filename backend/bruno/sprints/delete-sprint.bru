meta {
  name: Delete Sprint
  type: http
  seq: 10
}

delete {
  url: {{baseUrl}}/api/v1/squads/{{squadId}}/sprints/{{sprintId}}
  body: none
  auth: bearer
}

auth:bearer {
  token: {{accessToken}}
}

meta {
  name: Complete Sprint
  type: http
  seq: 7
}

post {
  url: {{baseUrl}}/api/v1/squads/{{squadId}}/sprints/{{sprintId}}/complete
  body: none
  auth: bearer
}

auth:bearer {
  token: {{accessToken}}
}

meta {
  name: Remove Item from Sprint
  type: http
  seq: 9
}

delete {
  url: {{baseUrl}}/api/v1/squads/{{squadId}}/sprints/{{sprintId}}/items/{{backlogItemId}}
  body: none
  auth: bearer
}

auth:bearer {
  token: {{accessToken}}
}

meta {
  name: Start Sprint
  type: http
  seq: 6
}

post {
  url: {{baseUrl}}/api/v1/squads/{{squadId}}/sprints/{{sprintId}}/start
  body: none
  auth: bearer
}

auth:bearer {
  token: {{accessToken}}
}

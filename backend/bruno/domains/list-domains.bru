meta {
  name: List Domains
  type: http
  seq: 1
}

get {
  url: {{baseUrl}}/api/v1/domains
  body: none
  auth: bearer
}

auth:bearer {
  token: {{accessToken}}
}

script:post-response {
  if (res.status === 200 && res.body.length > 0) {
    bru.setVar("domainId", res.body[0].id);
  }
}

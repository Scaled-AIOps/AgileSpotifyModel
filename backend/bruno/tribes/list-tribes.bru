meta {
  name: List Tribes
  type: http
  seq: 1
}

get {
  url: {{baseUrl}}/api/v1/tribes
  body: none
  auth: bearer
}

auth:bearer {
  token: {{accessToken}}
}

script:post-response {
  if (res.status === 200 && res.body.length > 0) {
    bru.setVar("tribeId", res.body[0].id);
  }
}

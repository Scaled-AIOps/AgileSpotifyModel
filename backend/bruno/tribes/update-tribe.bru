meta {
  name: Update Tribe
  type: http
  seq: 4
}

patch {
  url: {{baseUrl}}/api/v1/tribes/{{tribeId}}
  body: json
  auth: bearer
}

auth:bearer {
  token: {{accessToken}}
}

body:json {
  {
    "tribeName": "Search & Discovery",
    "description": "Updated description",
    "github": [
      { "url": "https://github.com/example/search-platform", "description": "Mono-repo" }
    ]
  }
}
